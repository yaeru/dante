{
  "name": "dante-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "push-dir --dir=dist --dotfiles --branch=gh-pages --clean"
  },
  "dependencies": {
    "nuxt": "^3.10.1",
    "push-dir": "^0.4.1",
    "sass": "^1.70.0",
    "sass-loader": "^14.1.0",
    "vue": "^3.4.15",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@samk-dev/nuxt-uikit3": "^1.1.5"
  }
}
